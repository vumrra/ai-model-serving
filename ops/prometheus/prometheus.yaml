global:
  scrape_interval: 10s

rule_files:
  - /etc/prometheus/alerts.yaml

scrape_configs:
  - job_name: qwen-gateway-local
    metrics_path: /metrics
    static_configs:
      - targets: [gateway:8080]
